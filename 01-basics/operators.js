//Arithmetic: + - / * % **  ex: 1+2=3;+ can add numbers or concatenate strings
// "hj" + "jk"="hjjk".12%2=0;
//comparision 

//== compare but not strict 12 == "12" it will give true 
//=== strict comparison, above example gives false
//!= not equal but not strictex:12!=13 true
//!== 12 !=="12" true
//>= greaterthanequal
//<=, <,>

//= assignment operator
//let a =12;
//a+=3;a-=3;a*=2;a/=2;a%=3;
//logical
//&&  || !
// && (AND): true only if both operands are true
// || (OR): true if at least one operand is true

//unary opeartor
//+ - ! typeof ++ --
//+"5" will convert to number 5
// let a = 12;
// ++a -> increments first, then returns 13
// a++ -> returns 12 first, then increments to 13

//ternary ?: ex:condition ? true hui ka code : false ka code

// typeof -> used for primitive data types
// instanceof -> used for reference types (Array, Object, Function)

// typeof 5; // "number"
// [] instanceof Array; // true
// {} instanceof Object; // true
